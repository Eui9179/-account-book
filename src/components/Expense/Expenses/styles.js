import styled from 'styled-components';
import Card from '../../UI/Card';

export const StyledExpenseList = styled(Card)`
  padding: 1rem;
  background-color: rgb(231, 231, 231);
  margin: 2rem auto;
  width: 50rem;
  max-width: 95%;
`;
