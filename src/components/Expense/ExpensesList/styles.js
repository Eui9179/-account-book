import styled from 'styled-components';

const StyledExpensesLissFallback = styled.p`
  margin-top: 30px;
  text-align: center;
  font-size: 1.4rem;
  font-weight: bold;
`;

const StyledExpenseList = styled.ul`
  list-style: none;
  padding: 0;
`;

export { StyledExpensesLissFallback, StyledExpenseList };
